{
  "name": "ascent-update-server",
  "version": "2.0.0",
  "private": true,
  "description": "Serves latest-version info for Ascent Compliance OS's in-app updater (deployed on Vercel as a serverless function — installers themselves live on GitHub Releases).",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
